port = 5815

# DB settings
db {
  default {
    url = "jdbc:postgresql://localhost/andon"
    driver = "org.postgresql.Driver"
    user = "amutake"
    password = ""
  }
}

jwt {
  secretKey = "secret"
  algorithm = "HS256"
}
